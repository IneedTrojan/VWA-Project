#pragma once
#include <functional>
#include <memory>
#include <set>
#include <unordered_map>
#include <vector>
#include "GraphNode.h"


namespace renderer
{
	

	/*
	class RenderPipeline
	{
		GraphNode* _chain;
	public:
		RenderPipeline() : _chain(nullptr)
		{

		}
		void initialize(GraphNode* main)
		{
			_chain = main;
		}

		void Render()const
		{
			assert(_chain != nullptr);
			_chain->Execute();
		}
	};*/
}
